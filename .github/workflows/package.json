{
  "name": "time-fleet-mobile",
  "version": "1.0.0",
  "description": "Time Fleet - App Mobile de Solicitação de O.S.",
  "scripts": {
    "build": "node build.js",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "deploy": "node build.js && npx cap sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/network": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
